name: tts_app
description: A simple TTS app using Flutter

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  pdf_render: ^0.77.0
  flutter_tts: ^3.3.0
  file_picker: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
